1. create a python virtual environment (and then enter it)
2. install dependencies (pip install -r backend/requirements.txt && npm install in frontend directory)
3. run backend && frontend in two separate terminals with `make run-backend` and `make run-frontend`